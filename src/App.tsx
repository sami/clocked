// Placeholder shell. The day view comes after the rules engine is done and
// tested: see "Milestones" in the README.
export default function App() {
  return (
    <main className="mx-auto max-w-xl px-4 py-10">
      <h1 className="text-2xl font-semibold tracking-tight">Clocked</h1>
      <p className="mt-2 text-ink-muted">
        A timesheet calculator for clockings that arrive incomplete. The rules
        engine is being built first, so there's nothing to type into yet.
      </p>
    </main>
  )
}
